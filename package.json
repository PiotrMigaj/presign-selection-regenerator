{
  "name": "presigned-url-regenerator",
  "version": "1.0.0",
  "description": "AWS SAM application to regenerate presigned URLs for DynamoDB table items",
  "main": "src/handler.mjs",
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS='--experimental-vm-modules' jest",
    "test:watch": "NODE_OPTIONS='--experimental-vm-modules' jest --watch",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix"
  },
  "keywords": [
    "aws",
    "sam",
    "lambda",
    "dynamodb",
    "s3",
    "presigned-urls",
    "eventbridge",
    "ses"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.485.0",
    "@aws-sdk/client-s3": "^3.485.0",
    "@aws-sdk/client-ses": "^3.485.0",
    "@aws-sdk/lib-dynamodb": "^3.485.0",
    "@aws-sdk/s3-request-presigner": "^3.485.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "eslint": "^8.54.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}